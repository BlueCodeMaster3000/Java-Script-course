console.log("Hello World")

    let a = 4  // variable example
    console.log(a)
    console.log(typeof(a)) // command to see data type of variable

    let b = 234.6
    console.log(typeof(b))

    let c = "Kacper Biegajło"
    console.log(typeof(c))

    let required = true
    console.log(typeof(required))
    // null and undefined are two other data types

    // let c = a+b will not work
    // var c = a+b this will work
    c = a+b // reassining is allowed with let keyword
    console.log(typeof(c))
    // we cannot redeclare variable with let but pissible with var
    console.log(!required) // reversed with ! to negative

    // const cannot be redeclared 

    //var - gerneral keyword / (let, const)ES6 keywords

// comments here 
/* 
Also comments but in multiple lines
*/